---
services:
    blocky:
        image: ghcr.io/0xerr0r/blocky:v0.24
        container_name: blocky
        environment:
            - TZ=America/Chicago
        ports:
            - 53:53/tcp
            - 53:53/udp
        restart: always
        volumes:
            - ./config.yml:/app/config.yml

volumes:
    tailscale:
