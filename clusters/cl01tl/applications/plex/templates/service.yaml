apiVersion: v1
kind: Service
metadata:
  name: plex-external
spec:
  selector:
    app: my-app
  type: NodePort
  ports:
  - name: http
    port: 32400
    targetPort: 32400
    nodePort: 32400
    protocol: TCP
