apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: grimmory-config-backup
  namespace: grimmory
  labels:
    app.kubernetes.io/name: grimmory-config-backup
    helm.sh/chart: volsync-target-config-2.3.0
    app.kubernetes.io/instance: grimmory
    app.kubernetes.io/part-of: grimmory
    app.kubernetes.io/version: "2.3.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            kube_pod_status_phase{namespace="grimmory", phase="Failed", pod=~"volsync-.*grimmory-config-backup-.*"} == 1
            * on(namespace, pod) group_left(owner_name)
            kube_pod_owner{namespace="grimmory", owner_kind="Job", owner_name=~"volsync-.*grimmory-config-backup-.*"}
          labels:
            severity: critical
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              VolSync backup pod '{{ $labels.pod }}' for PVC 'grimmory-config' failed.
              Job: {{ $labels.owner_name }}
              Namespace: {{ $labels.namespace }}
