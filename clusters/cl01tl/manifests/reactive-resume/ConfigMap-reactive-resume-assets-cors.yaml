apiVersion: v1
kind: ConfigMap
metadata:
  name: reactive-resume-assets-cors
  namespace: reactive-resume
  labels:
    app.kubernetes.io/name: reactive-resume-assets-cors
    helm.sh/chart: garage-bucket-reactive-resume-assets-0.3.10
    app.kubernetes.io/instance: reactive-resume
    app.kubernetes.io/part-of: reactive-resume
    app.kubernetes.io/version: "v0.7.5"
    app.kubernetes.io/managed-by: Helm
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
    "helm.sh/hook-weight": "-5"
data:
  cors.json: |-
    {
      "CORSRules": [
        {
          "AllowedOrigins": ["https://resume.alexlebens.net"],
          "AllowedHeaders": ["*"],
          "AllowedMethods": ["GET", "PUT", "POST", "DELETE", "HEAD"],
          "MaxAgeSeconds": 3000,
          "ExposeHeaders": ["ETag"]
        }
      ]
    }
