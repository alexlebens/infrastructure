apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: garage-cluster-b
  namespace: garage-operator
  labels:
    app.kubernetes.io/name: garage-cluster-b
    app.kubernetes.io/instance: garage-operator
    app.kubernetes.io/part-of: garage-operator
spec:
  selector:
    matchLabels:
      garage.rajsingh.info/cluster: garage-cluster-b
  endpoints:
    - port: admin
      interval: 5m
      scrapeTimeout: 2m
      bearerTokenSecret:
        name: garage-operator-token
        key: metrics-token
