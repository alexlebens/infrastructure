apiVersion: apps/v1
kind: Deployment
metadata:
  name: foldergram-main
  labels:
    app.kubernetes.io/controller: main
    app.kubernetes.io/instance: foldergram
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: foldergram
    helm.sh/chart: foldergram-5.2.1
  namespace: foldergram
spec:
  revisionHistoryLimit: 3
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/controller: main
      app.kubernetes.io/name: foldergram
      app.kubernetes.io/instance: foldergram
  template:
    metadata:
      labels:
        app.kubernetes.io/controller: main
        app.kubernetes.io/instance: foldergram
        app.kubernetes.io/name: foldergram
    spec:
      enableServiceLinks: false
      serviceAccountName: foldergram
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
      hostIPC: false
      hostNetwork: false
      hostPID: false
      dnsPolicy: ClusterFirst
      containers:
        - env:
            - name: IMAGE_DETAIL_SOURCE
              value: original
            - name: DERIVATIVE_MODE
              value: lazy
            - name: DATA_ROOT
              value: ./data
            - name: GALLERY_ROOT
              value: /gallery
            - name: CSRF_TRUSTED_ORIGINS
              value: https://art.alexlebens.dev
          image: ghcr.io/foldergram/foldergram:1.2.2@sha256:f72dfe39d9042555fd1b509a06f6c93c8b9a8be02541c80135a9a8f25fe362d4
          livenessProbe:
            httpGet:
              path: /api/health
              port: 4141
          name: main
          readinessProbe:
            httpGet:
              path: /api/health
              port: 4141
          resources:
            requests:
              cpu: 1m
              memory: 200Mi
          volumeMounts:
            - mountPath: /app/data
              name: data
            - mountPath: /app/data/db
              name: db
            - mountPath: /gallery/Favorites
              name: gallery-favorites
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: foldergram-cache
        - name: db
          persistentVolumeClaim:
            claimName: foldergram-db
        - name: gallery-favorites
          persistentVolumeClaim:
            claimName: foldergram-gallery-favorites
