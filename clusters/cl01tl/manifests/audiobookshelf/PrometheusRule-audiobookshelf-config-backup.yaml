apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: audiobookshelf-config-backup
  namespace: audiobookshelf
  labels:
    app.kubernetes.io/name: audiobookshelf-config-backup
    helm.sh/chart: volsync-target-config-2.4.0
    app.kubernetes.io/instance: audiobookshelf
    app.kubernetes.io/part-of: audiobookshelf
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="audiobookshelf", phase="Failed", pod=~"volsync-.*audiobookshelf-config-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="audiobookshelf", owner_kind="Job", owner_name=~"volsync-.*audiobookshelf-config-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: audiobookshelf-config
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'audiobookshelf-config'.
              Target: audiobookshelf-config-backup
              Namespace: {{ $labels.namespace }}
