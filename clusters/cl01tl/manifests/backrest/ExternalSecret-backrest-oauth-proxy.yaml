apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: backrest-oauth-proxy
  namespace: backrest
  labels:
    app.kubernetes.io/name: backrest-oauth-proxy
    app.kubernetes.io/instance: backrest
    app.kubernetes.io/part-of: backrest
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  data:
    - secretKey: OAUTH2_PROXY_CLIENT_ID
      remoteRef:
        key: /cl01tl/pocket-id/oidc/backrest
        property: clientId
    - secretKey: OAUTH2_PROXY_CLIENT_SECRET
      remoteRef:
        key: /cl01tl/pocket-id/oidc/backrest
        property: clientSecret
    - secretKey: OAUTH2_PROXY_COOKIE_SECRET
      remoteRef:
        key: /cl01tl/pocket-id/oidc/backrest
        property: cookie
    - secretKey: OAUTH2_PROXY_OIDC_ISSUER_URL
      remoteRef:
        key: /cl01tl/pocket-id/config
        property: issuer-url
