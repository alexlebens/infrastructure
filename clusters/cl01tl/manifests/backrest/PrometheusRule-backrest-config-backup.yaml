apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: backrest-config-backup
  namespace: backrest
  labels:
    app.kubernetes.io/name: backrest-config-backup
    helm.sh/chart: volsync-target-config-2.4.0
    app.kubernetes.io/instance: backrest
    app.kubernetes.io/part-of: backrest
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="backrest", phase="Failed", pod=~"volsync-.*backrest-config-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="backrest", owner_kind="Job", owner_name=~"volsync-.*backrest-config-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: backrest-config
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'backrest-config'.
              Target: backrest-config-backup
              Namespace: {{ $labels.namespace }}
