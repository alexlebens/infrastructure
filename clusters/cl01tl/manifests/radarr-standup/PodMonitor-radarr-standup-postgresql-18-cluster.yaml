apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: radarr-standup-postgresql-18-cluster
  namespace: radarr-standup
  labels:
    app.kubernetes.io/name: radarr-standup-postgresql-18-cluster
    helm.sh/chart: postgres-18-cluster-7.14.0
    app.kubernetes.io/instance: radarr-standup
    app.kubernetes.io/part-of: radarr-standup
    app.kubernetes.io/version: "7.14.0"
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: radarr-standup-postgresql-18-cluster
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
