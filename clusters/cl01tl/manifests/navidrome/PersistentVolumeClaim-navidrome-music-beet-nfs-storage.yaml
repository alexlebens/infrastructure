apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: navidrome-music-beet-nfs-storage
  namespace: navidrome
  labels:
    app.kubernetes.io/name: navidrome-music-beet-nfs-storage
    app.kubernetes.io/instance: navidrome
    app.kubernetes.io/part-of: navidrome
spec:
  volumeName: navidrome-music-beet-nfs-storage
  storageClassName: nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
