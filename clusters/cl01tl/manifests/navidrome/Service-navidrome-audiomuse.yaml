apiVersion: v1
kind: Service
metadata:
  name: navidrome-audiomuse
  labels:
    app.kubernetes.io/instance: navidrome
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: navidrome
    app.kubernetes.io/service: navidrome-audiomuse
    helm.sh/chart: navidrome-5.0.1
  namespace: navidrome
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/controller: audiomuse
    app.kubernetes.io/instance: navidrome
    app.kubernetes.io/name: navidrome
