apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: rclone-web-assets-remote-rclone
  labels:
    app.kubernetes.io/instance: rclone
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: web-assets-remote-rclone
    app.kubernetes.io/version: v1.74.3
    helm.sh/chart: rclone-web-assets-remote-0.12.5
  namespace: rclone
spec:
  jobLabel: app.kubernetes.io/name
  namespaceSelector:
    matchNames:
      - rclone
  selector:
    matchLabels:
      app.kubernetes.io/instance: rclone
      app.kubernetes.io/name: web-assets-remote-rclone
  endpoints:
    - interval: 10s
      path: /metrics
      port: rc-sync
      relabelings:
        - action: drop
          regex: Succeeded|Failed
          sourceLabels:
            - __meta_kubernetes_pod_phase
      scrapeTimeout: 5s
