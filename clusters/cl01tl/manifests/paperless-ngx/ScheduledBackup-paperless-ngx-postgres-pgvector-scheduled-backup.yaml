apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: paperless-ngx-postgres-pgvector-scheduled-backup
  namespace: paperless-ngx
  labels:
    app.kubernetes.io/name: paperless-ngx-postgres-pgvector-scheduled-backup
    helm.sh/chart: postgres-pgvector-cluster-8.4.3
    app.kubernetes.io/instance: paperless-ngx
    app.kubernetes.io/part-of: paperless-ngx
    app.kubernetes.io/version: "v1.30.0"
    app.kubernetes.io/managed-by: Helm
spec:
  immediate: true
  suspend: false
  schedule: "0 0 0 * * *"
  backupOwnerReference: self
  cluster:
    name: paperless-ngx-postgres-pgvector-cluster
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
    parameters:
      barmanObjectName: paperless-ngx-postgres-pgvector-backup
