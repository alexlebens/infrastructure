apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: paperless-ngx-postgres-pgvector-backup
  namespace: paperless-ngx
  labels:
    app.kubernetes.io/name: paperless-ngx-postgres-pgvector-backup
    helm.sh/chart: postgres-pgvector-cluster-8.4.3
    app.kubernetes.io/instance: paperless-ngx
    app.kubernetes.io/part-of: paperless-ngx
    app.kubernetes.io/version: "v1.30.0"
    app.kubernetes.io/managed-by: Helm
spec:
  retentionPolicy: 7d
  instanceSidecarConfiguration:
    env:
      - name: AWS_REQUEST_CHECKSUM_CALCULATION
        value: when_required
      - name: AWS_RESPONSE_CHECKSUM_VALIDATION
        value: when_required
  configuration:
    destinationPath: s3://postgres-backups/cl01tl/paperless-ngx/paperless-ngx-postgres-pgvector-cluster
    endpointURL: http://garage-cluster-a.garage-operator.svc.cluster.local:3900
    wal:
      compression: snappy
      maxParallel: 4
    data:
      compression: snappy
      jobs: 4
    s3Credentials:
      accessKeyId:
        name: paperless-ngx-postgres-pgvector-backup-secret
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: paperless-ngx-postgres-pgvector-backup-secret
        key: ACCESS_SECRET_KEY
      region:
        name: paperless-ngx-postgres-pgvector-backup-secret
        key: ACCESS_REGION
