apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: directus-assets-remote-rclone-ntfy
  namespace: directus
  labels:
    helm.sh/chart: rclone-directus-assets-remote-0.12.5
    app.kubernetes.io/instance: directus
    app.kubernetes.io/part-of: directus
    app.kubernetes.io/version: "0.12.5"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: directus-assets-remote-rclone-ntfy
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    template:
      mergePolicy: Merge
      engineVersion: v2
      data:
        ntfy-url: "{{ .url }}/{{ .topic }}"
        ntfy-token: "{{ .token }}"
  data:
    - secretKey: url
      remoteRef:
        key: /cl01tl/ntfy/config
        property: internal-endpoint
    - secretKey: topic
      remoteRef:
        key: /cl01tl/ntfy/topics
        property: rclone
    - secretKey: token
      remoteRef:
        key: /cl01tl/ntfy/users/cl01tl
        property: token
