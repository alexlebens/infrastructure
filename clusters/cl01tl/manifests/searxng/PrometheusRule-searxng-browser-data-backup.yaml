apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: searxng-browser-data-backup
  namespace: searxng
  labels:
    app.kubernetes.io/name: searxng-browser-data-backup
    helm.sh/chart: volsync-target-data-2.4.0
    app.kubernetes.io/instance: searxng
    app.kubernetes.io/part-of: searxng
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="searxng", phase="Failed", pod=~"volsync-.*searxng-browser-data-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="searxng", owner_kind="Job", owner_name=~"volsync-.*searxng-browser-data-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: searxng-browser-data
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'searxng-browser-data'.
              Target: searxng-browser-data-backup
              Namespace: {{ $labels.namespace }}
