apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: web-assets-secret
  namespace: web-assets
  labels:
    app.kubernetes.io/name: web-assets-secret
    helm.sh/chart: s3-bucket-0.4.0
    app.kubernetes.io/name: web-assets
    app.kubernetes.io/instance: web-assets
    app.kubernetes.io/version: "v1.75.1"
    app.kubernetes.io/managed-by: Helm
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      data:
        AWS_ACCESS_KEY_ID: "{{ .accessKeyId }}"
        AWS_SECRET_ACCESS_KEY: "{{ .secretAccessKey }}"
        AWS_REGION: "{{ .region }}"
        access-key-id: "{{ .accessKeyId }}"
        secret-access-key: "{{ .secretAccessKey }}"
        region: "{{ .region }}"
        endpoint: "http://synology.alexlebens.dev:3900"
  data:
    - secretKey: accessKeyId
      remoteRef:
        key: /garage/home-infra/web-assets
        property: ACCESS_KEY_ID
    - secretKey: secretAccessKey
      remoteRef:
        key: /garage/home-infra/web-assets
        property: ACCESS_SECRET_KEY
    - secretKey: region
      remoteRef:
        key: /garage/home-infra/web-assets
        property: ACCESS_REGION
