apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: sonarr-4k-config-backup
  namespace: sonarr-4k
  labels:
    app.kubernetes.io/name: sonarr-4k-config-backup
    helm.sh/chart: volsync-target-config-2.3.0
    app.kubernetes.io/instance: sonarr-4k
    app.kubernetes.io/part-of: sonarr-4k
    app.kubernetes.io/version: "2.3.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            kube_pod_status_phase{namespace="sonarr-4k", phase="Failed", pod=~"volsync-.*sonarr-4k-config-backup-.*"} == 1
            * on(namespace, pod) group_left(owner_name)
            kube_pod_owner{namespace="sonarr-4k", owner_kind="Job", owner_name=~"volsync-.*sonarr-4k-config-backup-.*"}
          labels:
            severity: critical
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              VolSync backup pod '{{ $labels.pod }}' for PVC 'sonarr-4k-config' failed.
              Job: {{ $labels.owner_name }}
              Namespace: {{ $labels.namespace }}
