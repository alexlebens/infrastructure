apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sp-desktop
  namespace: shelly-plug
  annotations:
    external-dns.alpha.kubernetes.io/target: "traefik-cl01tl.alexlebens.dev"
  labels:
    app.kubernetes.io/name: sp-desktop
    app.kubernetes.io/instance: shelly-plug
    app.kubernetes.io/part-of: shelly-plug
spec:
  entryPoints:
    - websecure
  routes:
    - match: "Host(`sp-desktop.alexlebens.dev`)"
      kind: Rule
      services:
        - name: shelly-plug-sp-desktop
          port: 80
  tls: {}
