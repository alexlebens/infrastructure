apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: shelly-plug-it07sp-config-secret
  namespace: shelly-plug
  labels:
    app.kubernetes.io/name: shelly-plug-it07sp-config-secret
    app.kubernetes.io/instance: shelly-plug
    app.kubernetes.io/part-of: shelly-plug
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  data:
    - secretKey: SHELLY_HTTP_USERNAME
      remoteRef:
        key: /it07sp/auth
        property: username
    - secretKey: SHELLY_HTTP_PASSWORD
      remoteRef:
        key: /it07sp/auth
        property: password
