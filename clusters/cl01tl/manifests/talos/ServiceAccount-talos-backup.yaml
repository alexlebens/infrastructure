apiVersion: v1
kind: ServiceAccount
metadata:
  name: talos-backup
  namespace: talos
  labels:
    app.kubernetes.io/name: talos-backup
    app.kubernetes.io/instance: talos
    app.kubernetes.io/part-of: talos
