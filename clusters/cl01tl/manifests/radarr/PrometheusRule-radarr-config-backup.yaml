apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: radarr-config-backup
  namespace: radarr
  labels:
    app.kubernetes.io/name: radarr-config-backup
    helm.sh/chart: volsync-target-config-2.3.0
    app.kubernetes.io/instance: radarr
    app.kubernetes.io/part-of: radarr
    app.kubernetes.io/version: "2.3.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            kube_pod_status_phase{namespace="radarr", phase="Failed", pod=~"volsync-.*radarr-config-backup-.*"} == 1
            * on(namespace, pod) group_left(owner_name)
            kube_pod_owner{namespace="radarr", owner_kind="Job", owner_name=~"volsync-.*radarr-config-backup-.*"}
          labels:
            severity: critical
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              VolSync backup pod '{{ $labels.pod }}' for PVC 'radarr-config' failed.
              Job: {{ $labels.owner_name }}
              Namespace: {{ $labels.namespace }}
