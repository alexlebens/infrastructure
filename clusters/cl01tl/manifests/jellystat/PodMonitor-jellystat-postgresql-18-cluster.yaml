apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: jellystat-postgresql-18-cluster
  namespace: jellystat
  labels:
    app.kubernetes.io/name: jellystat-postgresql-18-cluster
    helm.sh/chart: postgres-18-cluster-7.14.0
    app.kubernetes.io/instance: jellystat
    app.kubernetes.io/part-of: jellystat
    app.kubernetes.io/version: "7.14.0"
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: jellystat-postgresql-18-cluster
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
