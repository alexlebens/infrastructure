apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: immich-postgresql-18-cluster
  namespace: immich
  labels:
    app.kubernetes.io/name: immich-postgresql-18-cluster
    helm.sh/chart: postgres-18-cluster-7.15.2
    app.kubernetes.io/instance: immich
    app.kubernetes.io/part-of: immich
    app.kubernetes.io/version: "7.15.2"
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: immich-postgresql-18-cluster
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
