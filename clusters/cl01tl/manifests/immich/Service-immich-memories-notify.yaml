apiVersion: v1
kind: Service
metadata:
  name: immich-memories-notify
  labels:
    app.kubernetes.io/instance: immich
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: immich
    app.kubernetes.io/service: immich-memories-notify
    helm.sh/chart: immich-5.1.0
  namespace: immich
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/controller: immich-memories-notify
    app.kubernetes.io/instance: immich
    app.kubernetes.io/name: immich
