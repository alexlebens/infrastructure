apiVersion: v1
kind: Service
metadata:
  name: openbao-openbao-backups-external-rclone
  labels:
    app.kubernetes.io/instance: openbao
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: openbao-backups-external-rclone
    app.kubernetes.io/service: openbao-openbao-backups-external-rclone
    app.kubernetes.io/version: v1.74.2
    helm.sh/chart: rclone-openbao-backups-external-0.11.6
  namespace: openbao
spec:
  type: ClusterIP
  ports:
    - port: 5573
      targetPort: 5573
      protocol: TCP
      name: rc-prune
    - port: 5572
      targetPort: 5572
      protocol: TCP
      name: rc-sync
  selector:
    app.kubernetes.io/controller: main
    app.kubernetes.io/instance: openbao
    app.kubernetes.io/name: openbao-backups-external-rclone
