apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openbao-backups-secret-backup-ntfy
  namespace: openbao
  labels:
    app.kubernetes.io/name: openbao-backups-secret-backup-ntfy
    helm.sh/chart: garage-bucket-openbao-backups-0.3.10
    app.kubernetes.io/instance: openbao
    app.kubernetes.io/part-of: openbao
    app.kubernetes.io/version: "v0.7.5"
    app.kubernetes.io/managed-by: Helm
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    template:
      mergePolicy: Merge
      engineVersion: v2
      data:
        ntfy-url: "{{ .url }}/{{ .topic }}"
        ntfy-token: "{{ .token }}"
  data:
    - secretKey: url
      remoteRef:
        key: /cl01tl/ntfy/config
        property: internal-endpoint
    - secretKey: topic
      remoteRef:
        key: /cl01tl/ntfy/topics
        property: rclone
    - secretKey: token
      remoteRef:
        key: /cl01tl/ntfy/users/cl01tl
        property: token
