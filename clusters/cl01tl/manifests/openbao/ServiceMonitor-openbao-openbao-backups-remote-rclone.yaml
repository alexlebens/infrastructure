apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: openbao-openbao-backups-remote-rclone
  labels:
    app.kubernetes.io/instance: openbao
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: openbao-backups-remote-rclone
    app.kubernetes.io/version: v1.74.2
    helm.sh/chart: rclone-openbao-backups-remote-0.11.6
  namespace: openbao
spec:
  jobLabel: app.kubernetes.io/name
  namespaceSelector:
    matchNames:
      - openbao
  selector:
    matchLabels:
      app.kubernetes.io/instance: openbao
      app.kubernetes.io/name: openbao-backups-remote-rclone
  endpoints:
    - interval: 10s
      path: /metrics
      port: rc-sync
      scrapeTimeout: 5s
    - interval: 10s
      path: /metrics
      port: rc-prune
      scrapeTimeout: 5s
