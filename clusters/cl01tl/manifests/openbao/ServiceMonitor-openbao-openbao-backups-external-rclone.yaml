apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: openbao-openbao-backups-external-rclone
  labels:
    app.kubernetes.io/instance: openbao
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: openbao-backups-external-rclone
    app.kubernetes.io/version: v1.74.1
    helm.sh/chart: rclone-openbao-backups-external-0.11.5
  namespace: openbao
spec:
  jobLabel: app.kubernetes.io/name
  namespaceSelector:
    matchNames:
      - openbao
  selector:
    matchLabels:
      app.kubernetes.io/instance: openbao
      app.kubernetes.io/name: openbao-backups-external-rclone
  endpoints:
    - interval: 10s
      path: /metrics
      port: rc-sync
      scrapeTimeout: 5s
    - interval: 10s
      path: /metrics
      port: rc-prune
      scrapeTimeout: 5s
