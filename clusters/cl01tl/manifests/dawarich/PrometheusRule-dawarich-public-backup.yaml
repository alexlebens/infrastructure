apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: dawarich-public-backup
  namespace: dawarich
  labels:
    app.kubernetes.io/name: dawarich-public-backup
    helm.sh/chart: volsync-target-public-2.4.0
    app.kubernetes.io/instance: dawarich
    app.kubernetes.io/part-of: dawarich
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="dawarich", phase="Failed", pod=~"volsync-.*dawarich-public-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="dawarich", owner_kind="Job", owner_name=~"volsync-.*dawarich-public-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: dawarich-public
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'dawarich-public'.
              Target: dawarich-public-backup
              Namespace: {{ $labels.namespace }}
