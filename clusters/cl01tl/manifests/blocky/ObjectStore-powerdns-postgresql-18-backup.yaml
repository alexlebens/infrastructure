apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: powerdns-postgresql-18-backup
  namespace: blocky
  labels:
    app.kubernetes.io/name: powerdns-postgresql-18-backup
    helm.sh/chart: powerdns-postgres-18-cluster-8.2.3
    app.kubernetes.io/instance: blocky
    app.kubernetes.io/part-of: blocky
    app.kubernetes.io/version: "v1.30.0"
    app.kubernetes.io/managed-by: Helm
spec:
  retentionPolicy: 7d
  instanceSidecarConfiguration:
    env:
      - name: AWS_REQUEST_CHECKSUM_CALCULATION
        value: when_required
      - name: AWS_RESPONSE_CHECKSUM_VALIDATION
        value: when_required
  configuration:
    destinationPath: s3://postgres-backups/cl01tl/blocky/powerdns-postgresql-18-cluster
    endpointURL: http://garage-cluster-a.garage-operator.svc.cluster.local:3900
    wal:
      compression: snappy
      maxParallel: 4
    data:
      compression: snappy
      jobs: 4
    s3Credentials:
      accessKeyId:
        name: powerdns-postgresql-18-backup-secret
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: powerdns-postgresql-18-backup-secret
        key: ACCESS_SECRET_KEY
      region:
        name: powerdns-postgresql-18-backup-secret
        key: ACCESS_REGION
