apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: powerdns-postgresql-18-recovery
  namespace: blocky
  labels:
    app.kubernetes.io/name: powerdns-postgresql-18-recovery
    helm.sh/chart: powerdns-postgres-18-cluster-8.4.3
    app.kubernetes.io/instance: blocky
    app.kubernetes.io/part-of: blocky
    app.kubernetes.io/version: "v1.30.0"
    app.kubernetes.io/managed-by: Helm
spec:
  configuration:
    destinationPath: s3://postgres-backups/cl01tl/blocky/powerdns-postgresql-18-cluster
    endpointURL: http://garage-cluster-a.garage-operator.svc.cluster.local:3900
    wal:
      compression: snappy
      maxParallel: 1
    data:
      compression: snappy
      jobs: 1
    s3Credentials:
      accessKeyId:
        name: powerdns-postgresql-18-recovery-secret
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: powerdns-postgresql-18-recovery-secret
        key: ACCESS_SECRET_KEY
      region:
        name: powerdns-postgresql-18-recovery-secret
        key: ACCESS_REGION
