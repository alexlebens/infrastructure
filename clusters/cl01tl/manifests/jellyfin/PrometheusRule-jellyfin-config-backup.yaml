apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: jellyfin-config-backup
  namespace: jellyfin
  labels:
    app.kubernetes.io/name: jellyfin-config-backup
    helm.sh/chart: volsync-target-config-2.4.0
    app.kubernetes.io/instance: jellyfin
    app.kubernetes.io/part-of: jellyfin
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="jellyfin", phase="Failed", pod=~"volsync-.*jellyfin-config-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="jellyfin", owner_kind="Job", owner_name=~"volsync-.*jellyfin-config-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: jellyfin-config
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'jellyfin-config'.
              Target: jellyfin-config-backup
              Namespace: {{ $labels.namespace }}
