apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: qbittorrent-qbit-manage-config-data-backup-source-local
  namespace: qbittorrent
  labels:
    helm.sh/chart: volsync-target-qbit-manage-config-1.1.0
    app.kubernetes.io/instance: qbittorrent
    app.kubernetes.io/part-of: qbittorrent
    app.kubernetes.io/version: "1.1.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: qbittorrent-qbit-manage-config-data-backup-source-local
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            kube_pod_container_status_last_terminated_exit_code > 0
            * on(pod, namespace) group_left(owner_name)
            label_replace(
              kube_pod_owner{owner_kind="Job"},
              "owner_name", "$1", "owner_name", "(.*)"
            )
          for: 1m
          labels:
            severity: critical
          annotations:
            summary: "VolSync Backup Pod failed in {{ $labels.namespace }}"
            description: |
              A pod for the VolSync backup of PVC 'qbittorrent-qbit-manage-config-data' failed with exit code {{ $value }}.
              Job: {{ $labels.owner_name }}
              Namespace: {{ $labels.namespace }}
