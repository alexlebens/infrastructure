apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: qbittorrent-qui-config-data-backup
  namespace: qbittorrent
  labels:
    app.kubernetes.io/name: qbittorrent-qui-config-data-backup
    helm.sh/chart: volsync-target-qui-config-2.3.1
    app.kubernetes.io/instance: qbittorrent
    app.kubernetes.io/part-of: qbittorrent
    app.kubernetes.io/version: "2.3.1"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            (kube_pod_status_phase{namespace="qbittorrent", phase="Failed", pod=~"volsync-.*qbittorrent-qui-config-data-backup-.*"} == 1)
            * on(namespace, pod) group_left(owner_name)
            kube_pod_owner{namespace="qbittorrent", owner_kind="Job", owner_name=~"volsync-.*qbittorrent-qui-config-data-backup-.*"}
          labels:
            severity: critical
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              VolSync backup pod '{{ $labels.pod }}' for PVC 'qbittorrent-qui-config-data' failed.
              Job: {{ $labels.owner_name }}
              Namespace: {{ $labels.namespace }}
