apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: postiz-postgresql-18-cluster
  namespace: postiz
  labels:
    app.kubernetes.io/name: postiz-postgresql-18-cluster
    helm.sh/chart: postgres-18-cluster-7.15.0
    app.kubernetes.io/instance: postiz
    app.kubernetes.io/part-of: postiz
    app.kubernetes.io/version: "7.15.0"
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: postiz-postgresql-18-cluster
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
