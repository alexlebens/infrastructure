apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: postiz-config-backup
  namespace: postiz
  labels:
    app.kubernetes.io/name: postiz-config-backup
    helm.sh/chart: volsync-target-config-2.4.0
    app.kubernetes.io/instance: postiz
    app.kubernetes.io/part-of: postiz
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="postiz", phase="Failed", pod=~"volsync-.*postiz-config-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="postiz", owner_kind="Job", owner_name=~"volsync-.*postiz-config-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: postiz-config
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'postiz-config'.
              Target: postiz-config-backup
              Namespace: {{ $labels.namespace }}
