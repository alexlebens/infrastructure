apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: pocketidoidcclients.pocketid.internal
spec:
  group: pocketid.internal
  names:
    kind: PocketIDOIDCClient
    listKind: PocketIDOIDCClientList
    plural: pocketidoidcclients
    shortNames:
      - oidcclient
    singular: pocketidoidcclient
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: PocketIDOIDCClient is the Schema for the pocketidoidcclients API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: spec defines the desired state of PocketIDOIDCClient
              properties:
                accessTokenDurationMinutes:
                  default: 60
                  description: How long access tokens issued to this client stay valid, in minutes
                  format: int64
                  maximum: 525600
                  minimum: 1
                  type: integer
                allowedUserGroups:
                  description: |-
                    AllowedUserGroups restricts access to the listed user groups. Each entry names
                    either a PocketIDUserGroup CR or, for groups managed outside this cluster, an
                    existing Pocket-ID group by name or ID.
                  items:
                    description: |-
                      NamespacedUserGroupReference selects a Pocket-ID user group, either by the
                      PocketIDUserGroup CR that manages it or, for groups managed outside this cluster,
                      by their name or ID in Pocket-ID.
                    properties:
                      groupID:
                        description: |-
                          GroupID is the Pocket-ID ID of an existing user group. Like groupName, the
                          group itself is left untouched.
                        maxLength: 64
                        minLength: 1
                        type: string
                      groupName:
                        description: |-
                          GroupName is the name of an existing user group in Pocket-ID, for groups not
                          managed by a PocketIDUserGroup in this cluster. The operator only resolves the
                          group to grant this client access and never modifies the group itself.
                        maxLength: 255
                        minLength: 2
                        type: string
                      name:
                        description: Name is the name of the PocketIDUserGroup CR
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the PocketIDUserGroup CR
                          Defaults to the PocketIDOIDCClient namespace
                        maxLength: 63
                        minLength: 1
                        type: string
                    type: object
                    x-kubernetes-validations:
                      - message: exactly one of name, groupName, or groupID must be set
                        rule: '(has(self.name) ? 1 : 0) + (has(self.groupName) ? 1 : 0) + (has(self.groupID) ? 1 : 0) == 1'
                      - message: namespace may only be set together with name
                        rule: '!has(self.namespace) || has(self.name)'
                  type: array
                apiAccess:
                  description: |-
                    APIAccess grants this client permission to request scoped access on the
                    referenced PocketIDAPIs. The client is the sole owner of its API access in
                    Pocket-ID: while this field is set the operator keeps the access in sync and
                    clears it if the field is later emptied. When never set, existing API access
                    configured out-of-band is left untouched.
                  items:
                    description: |-
                      OIDCClientAPIAccess grants a client scoped permissions on a referenced PocketIDAPI.
                      Permissions are referenced by their key; the controller resolves them to Pocket-ID
                      permission IDs via the PocketIDAPI status.
                      The access flags exist to grant a flow that selects no permissions, so setting one to false
                      alongside a permission list is rejected rather than silently ignoring the list.
                    properties:
                      apiRef:
                        description: APIRef references the PocketIDAPI granting access.
                        properties:
                          name:
                            description: Name is the name of the PocketIDAPI CR
                            type: string
                          namespace:
                            description: |-
                              Namespace is the namespace of the PocketIDAPI CR
                              Defaults to the referencing resource's namespace
                            type: string
                        required:
                          - name
                        type: object
                      clientAccess:
                        description: |-
                          ClientAccess grants the client-credentials flow even with no permissions selected.
                          Defaults to true when clientPermissions is non-empty, and requires a confidential client.
                        type: boolean
                      clientPermissions:
                        description: |-
                          ClientPermissions are permission keys granted for the client-credentials
                          (machine-to-machine) flow. These require a confidential client.
                        items:
                          type: string
                        type: array
                      delegatedAccess:
                        description: |-
                          DelegatedAccess grants the user-delegated flow even with no permissions selected,
                          e.g. a client requesting a resource that doesn't need any scopes.
                          Defaults to true when delegatedPermissions is non-empty.
                        type: boolean
                      delegatedPermissions:
                        description: |-
                          DelegatedPermissions are permission keys granted for the user-delegated
                          (authorization code) flow, where the client acts on behalf of a user.
                        items:
                          type: string
                        type: array
                    required:
                      - apiRef
                    type: object
                    x-kubernetes-validations:
                      - message: 'delegatedAccess: false conflicts with delegatedPermissions; drop the permissions to revoke the flow, or remove the flag to grant them'
                        rule: '!has(self.delegatedAccess) || self.delegatedAccess || !has(self.delegatedPermissions) || size(self.delegatedPermissions) == 0'
                      - message: 'clientAccess: false conflicts with clientPermissions; drop the permissions to revoke the flow, or remove the flag to grant them'
                        rule: '!has(self.clientAccess) || self.clientAccess || !has(self.clientPermissions) || size(self.clientPermissions) == 0'
                  type: array
                callbackUrls:
                  description: Callback URLs for the client
                  items:
                    type: string
                  type: array
                clientID:
                  description: |-
                    ClientID is the optional OIDC client ID to use instead of a generated one.
                    The Client ID is immutable and cannot be changed once the oidc client is created.
                    An https:// URL marks the client as an OAuth Client ID Metadata Document (CIMD)
                    client, which the operator adopts but never creates and only partially manages.
                  maxLength: 512
                  minLength: 2
                  type: string
                clientSecretOverlap:
                  description: |-
                    ClientSecretOverlap is how long the client-secret a replacement supersedes stays valid in
                    Pocket-ID, giving consumers that cached it time to pick up the new one. It covers every
                    replacement: scheduled rotations, the regenerate-client-secret annotation, and clientSecretRef updates.

                    When unset or zero the previous secret is deleted as soon as the replacement is stored,
                    so exactly one secret is ever valid. Must not exceed 168h, nor clientSecretRotation.interval if set.
                  type: string
                  x-kubernetes-validations:
                    - message: clientSecretOverlap must not exceed 168h (7 days)
                      rule: duration(self) <= duration('168h')
                clientSecretRef:
                  description: |-
                    ClientSecretRef references a Kubernetes Secret key holding the client secret to set in
                    Pocket-ID, instead of letting the operator generate one. The value must be at least 16
                    printable ASCII characters. While set, the operator never generates or rotates the secret:
                    clientSecretRotation cannot be enabled and the regenerate-client-secret annotation is
                    ignored.
                  properties:
                    key:
                      description: The key of the secret to select from.  Must be a valid secret key.
                      type: string
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    optional:
                      description: Specify whether the Secret or its key must be defined
                      type: boolean
                  required:
                    - key
                  type: object
                  x-kubernetes-map-type: atomic
                clientSecretRotation:
                  description: |-
                    ClientSecretRotation configures automatic, scheduled regeneration of the client secret.
                    When unset the secret is only regenerated on create/adopt, on self-heal,
                    or when the regenerate-client-secret annotation is set.
                  properties:
                    enabled:
                      default: false
                      description: Enabled controls whether automatic rotation is active.
                      type: boolean
                    interval:
                      description: |-
                        Interval is the minimum time between rotations (e.g. "720h" for 30 days). Must be between
                        1h and 8760h. When omitted, rotation is driven entirely by window (which then becomes
                        required) and the secret rotates once per window opening.
                      type: string
                      x-kubernetes-validations:
                        - message: interval must be between 1h and 8760h (1 year)
                          rule: duration(self) >= duration('1h') && duration(self) <= duration('8760h')
                    window:
                      description: |-
                        Window restricts rotations to a recurring maintenance window. When interval is set, rotations
                        fire as soon as the interval and global min-spacing are satisfied, confined to the window
                        when one is configured. When interval is omitted, the window opening is itself the trigger:
                        the secret rotates once per opening (subject to min-spacing).
                      properties:
                        closesAfter:
                          description: ClosesAfter is how long the window stays open after each cron fire. Minimum 5m.
                          type: string
                          x-kubernetes-validations:
                            - message: closesAfter must be at least 5m
                              rule: duration(self) >= duration('5m')
                        opens:
                          description: Opens is a standard 5-field cron expression (e.g. "0 1 * * *" for 1am daily, local TZ).
                          type: string
                      required:
                        - closesAfter
                        - opens
                      type: object
                  required:
                    - enabled
                  type: object
                  x-kubernetes-validations:
                    - message: enabled rotation requires interval, window, or both
                      rule: '!self.enabled || has(self.interval) || has(self.window)'
                    - message: window.closesAfter must not exceed interval
                      rule: '!has(self.window) || !has(self.interval) || duration(self.window.closesAfter) <= duration(self.interval)'
                darkLogoUrl:
                  description: |-
                    Dark logo URL for the client
                    Deprecated: Use spec.logo.darkLogoUrl instead.
                  type: string
                description:
                  description: Description is an optional description for the client
                  maxLength: 150
                  type: string
                federatedIdentities:
                  description: FederatedIdentities configures JWT assertion identities
                  items:
                    description: OIDCClientFederatedIdentity defines a federated identity for OIDC client credentials.
                    properties:
                      audience:
                        description: Audience is the audience for the identity
                        type: string
                      issuer:
                        description: Issuer is the OIDC issuer for the identity
                        type: string
                      jwks:
                        description: JWKS is the URL or JSON for the identity's JWKS
                        type: string
                      replayProtection:
                        default: false
                        description: ReplayProtection requires client assertions to carry a jti and rejects replays
                        type: boolean
                      subject:
                        description: Subject is the subject for the identity
                        type: string
                    type: object
                  type: array
                instanceSelector:
                  description: |-
                    InstanceSelector selects the PocketIDInstance to reconcile against.
                    If omitted, the controller expects exactly one instance in the cluster.
                  properties:
                    matchExpressions:
                      description: matchExpressions is a list of label selector requirements. The requirements are ANDed.
                      items:
                        description: |-
                          A label selector requirement is a selector that contains values, a key, and an operator that
                          relates the key and values.
                        properties:
                          key:
                            description: key is the label key that the selector applies to.
                            type: string
                          operator:
                            description: |-
                              operator represents a key's relationship to a set of values.
                              Valid operators are In, NotIn, Exists and DoesNotExist.
                            type: string
                          values:
                            description: |-
                              values is an array of string values. If the operator is In or NotIn,
                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                              the values array must be empty. This array is replaced during a strategic
                              merge patch.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                          - key
                          - operator
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: |-
                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                      type: object
                  type: object
                  x-kubernetes-map-type: atomic
                isPublic:
                  default: false
                  description: Indicates if the client is public (no client secret)
                  type: boolean
                launchUrl:
                  description: Launch URL for the client
                  type: string
                logo:
                  description: Logo configures logo URLs for the OIDC client with template support.
                  properties:
                    autoGenerate:
                      description: |-
                        AutoGenerate controls whether to auto-generate logo URLs from templates
                        when logoUrl/darkLogoUrl are not explicitly set in this struct.
                        If not set, defaults to the AUTOGENERATE_LOGOS env var on the operator, which defaults to true.
                      type: boolean
                    darkLogoUrl:
                      description: |-
                        DarkLogoURL is the URL template for the dark logo.
                        Use {{name}} as a placeholder for the logo name.
                        Defaults to the DEFAULT_DARK_LOGO_URL env var on the operator.
                      type: string
                    logoUrl:
                      description: |-
                        LogoURL is the URL template for the light logo.
                        Use {{name}} as a placeholder for the logo name.
                        Defaults to the DEFAULT_LOGO_URL env var on the operator.
                      type: string
                    nameOverride:
                      description: |-
                        NameOverride overrides the logo name used in template substitution.
                        Defaults to metadata.name.
                      type: string
                  type: object
                logoUrl:
                  description: |-
                    Logo URL for the client
                    Deprecated: Use spec.logo.logoUrl instead.
                  type: string
                logoutCallbackUrls:
                  description: Logout callback URLs for the client
                  items:
                    type: string
                  type: array
                name:
                  description: |-
                    Name of the oidc client to create in Pocket ID.
                    If omitted, defaults to metadata.name of the oidcclient resource.
                  type: string
                pkceEnabled:
                  default: false
                  description: Enables PKCE for the client
                  type: boolean
                refreshTokenDurationMinutes:
                  default: 43200
                  description: How long refresh tokens issued to this client stay valid, in minutes
                  format: int64
                  maximum: 525600
                  minimum: 1
                  type: integer
                requiresPushedAuthorizationRequests:
                  default: false
                  description: Requires the client to use pushed authorization requests (PAR)
                  type: boolean
                requiresReauthentication:
                  default: false
                  description: Requires reauthentication on login
                  type: boolean
                scim:
                  description: |-
                    SCIM configures SCIM provisioning for this OIDC client.
                    Pocket ID will push user/group changes to the configured endpoint.
                  properties:
                    endpoint:
                      description: Endpoint is the URL of the external SCIM service provider.
                      type: string
                    tokenSecretRef:
                      description: |-
                        TokenSecretRef references a Kubernetes Secret key containing the
                        bearer token used to authenticate with the SCIM endpoint.
                        If omitted, no Authorization header is sent.
                      properties:
                        key:
                          description: The key of the secret to select from.  Must be a valid secret key.
                          type: string
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        optional:
                          description: Specify whether the Secret or its key must be defined
                          type: boolean
                      required:
                        - key
                      type: object
                      x-kubernetes-map-type: atomic
                  required:
                    - endpoint
                  type: object
                secret:
                  description: Secret defines how OIDC client credentials should be stored in a Kubernetes Secret.
                  properties:
                    additionalAnnotations:
                      additionalProperties:
                        type: string
                      description: |-
                        AdditionalAnnotations allows setting custom annotations on the secret created by the
                        PocketIDOIDCClient custom resource. Operator-managed annotations take precedence and
                        cannot be overridden.
                      type: object
                    additionalLabels:
                      additionalProperties:
                        type: string
                      description: |-
                        AdditionalLabels allows setting custom labels on the secret created by the PocketIDOIDCClient
                        custom resource
                      type: object
                    enabled:
                      default: true
                      description: |-
                        Enabled controls whether to create a secret with OIDC client credentials.
                        If false, no secret will be created. Defaults to true
                      type: boolean
                    keys:
                      description: Keys allows customization of the secret keys for each credential field.
                      properties:
                        authorizationUrl:
                          default: authorization_url
                          description: |-
                            AuthorizationURL is the key name for the OIDC authorization endpoint URL.
                            Defaults to authorization_url
                          type: string
                        callbackUrls:
                          default: callback_urls
                          description: |-
                            CallbackURLs is the key name for the callback URLs.
                            Defaults to callback_urls
                          type: string
                        clientID:
                          default: client_id
                          description: |-
                            ClientID is the key name for the OIDC client ID.
                            Defaults to client_id
                          type: string
                        clientSecret:
                          default: client_secret
                          description: |-
                            ClientSecret is the key name for the OIDC client secret.
                            Defaults to client_secret
                          type: string
                        discoveryUrl:
                          default: discovery_url
                          description: |-
                            DiscoveryURL is the key name for the OIDC discovery URL.
                            Defaults to discovery_url
                          type: string
                        endSessionUrl:
                          default: end_session_url
                          description: |-
                            EndSessionURL is the key name for the OIDC end session (logout) endpoint URL.
                            Defaults to end_session_url
                          type: string
                        issuerUrl:
                          default: issuer_url
                          description: |-
                            IssuerURL is the key name for the OIDC issuer URL.
                            Defaults to issuer_url
                          type: string
                        jwksUrl:
                          default: jwks_url
                          description: |-
                            JwksURL is the key name for the OIDC JWKS (JSON Web Key Set) endpoint URL.
                            Defaults to jwks_url
                          type: string
                        logoutCallbackUrls:
                          default: logout_callback_urls
                          description: |-
                            LogoutCallbackURLs is the key name for the logout callback URLs.
                            Defaults to logout_callback_urls
                          type: string
                        resource:
                          default: resource
                          description: |-
                            Resource is the key name for the audience to request tokens for, taken from the APIs
                            in apiAccess. It also prefixes the per-API key "<resource>_<apiRef.name>".
                            Defaults to resource
                          type: string
                        scopes:
                          default: scopes
                          description: |-
                            Scopes is the key name for the space-delimited scopes apiAccess grants the
                            client-credentials flow. It also prefixes the per-API key "<scopes>_<apiRef.name>".
                            Defaults to scopes
                          type: string
                        tokenUrl:
                          default: token_url
                          description: |-
                            TokenURL is the key name for the OIDC token endpoint URL.
                            Defaults to token_url
                          type: string
                        userinfoUrl:
                          default: userinfo_url
                          description: |-
                            UserinfoURL is the key name for the OIDC userinfo endpoint URL.
                            Defaults to userinfo_url
                          type: string
                      type: object
                    name:
                      description: |-
                        Name is the name of the secret to create.
                        Defaults to metadata.name + "-oidc-credentials"
                      type: string
                    storeClientSecret:
                      default: true
                      description: |-
                        StoreClientSecret controls whether the operator manages the client secret.
                        When false, the operator never regenerates an existing client secret: a client
                        created by the operator still gets its initial secret minted and stored, while
                        an adopted client keeps its externally-managed secret and the Secret omits the
                        client_secret key. Defaults to true
                      type: boolean
                  type: object
                skipConsent:
                  default: false
                  description: Skips the consent screen on initial authorization for this client
                  type: boolean
              type: object
              x-kubernetes-validations:
                - message: clientID is immutable
                  rule: has(self.clientID) == has(oldSelf.clientID) && (!has(self.clientID) || self.clientID == oldSelf.clientID)
                - message: clientSecretRotation cannot be enabled when secret.storeClientSecret is false
                  rule: '!has(self.clientSecretRotation) || !self.clientSecretRotation.enabled || !has(self.secret) || !has(self.secret.storeClientSecret) || self.secret.storeClientSecret'
                - message: clientPermissions and clientAccess require a confidential client (isPublic must be false)
                  rule: '!self.isPublic || !has(self.apiAccess) || self.apiAccess.all(a, (!has(a.clientPermissions) || size(a.clientPermissions) == 0) && (!has(a.clientAccess) || !a.clientAccess))'
                - message: clientSecretRef requires a confidential client (isPublic must be false)
                  rule: '!has(self.clientSecretRef) || !self.isPublic'
                - message: clientSecretRotation cannot be enabled when clientSecretRef is set
                  rule: '!has(self.clientSecretRef) || !has(self.clientSecretRotation) || !self.clientSecretRotation.enabled'
                - message: clientID must be at most 128 characters unless it is a client ID metadata document URL
                  rule: '!has(self.clientID) || self.clientID.startsWith(''https://'') || self.clientID.size() <= 128'
                - message: name, callbackUrls, logoutCallbackUrls, federatedIdentities, isPublic, and pkceEnabled are owned by the client ID metadata document and must not be set on a CIMD client
                  rule: '!(has(self.clientID) && self.clientID.startsWith(''https://'')) || (!has(self.name) && !has(self.callbackUrls) && !has(self.logoutCallbackUrls) && !has(self.federatedIdentities) && !self.isPublic && !self.pkceEnabled)'
                - message: a CIMD client is always public and has no client secret, so clientSecretRef and clientSecretRotation are not supported
                  rule: '!(has(self.clientID) && self.clientID.startsWith(''https://'')) || (!has(self.clientSecretRef) && (!has(self.clientSecretRotation) || !self.clientSecretRotation.enabled))'
                - message: clientPermissions and clientAccess require a confidential client, and a CIMD client is always public
                  rule: '!(has(self.clientID) && self.clientID.startsWith(''https://'')) || !has(self.apiAccess) || self.apiAccess.all(a, (!has(a.clientPermissions) || size(a.clientPermissions) == 0) && (!has(a.clientAccess) || !a.clientAccess))'
                - message: clientSecretOverlap must not exceed clientSecretRotation.interval
                  rule: '!has(self.clientSecretOverlap) || !has(self.clientSecretRotation) || !has(self.clientSecretRotation.interval) || duration(self.clientSecretOverlap) <= duration(self.clientSecretRotation.interval)'
                - message: clientSecretOverlap requires a confidential client (isPublic must be false)
                  rule: '!has(self.clientSecretOverlap) || !self.isPublic'
            status:
              description: status defines the observed state of PocketIDOIDCClient
              properties:
                allowedUserGroupIDs:
                  description: AllowedUserGroupIDs are the resolved group IDs assigned to the client
                  items:
                    type: string
                  type: array
                callbackUrls:
                  description: CallbackURLs are the current callback URLs from Pocket-ID
                  items:
                    type: string
                  type: array
                cimdGrantedAPIs:
                  description: |-
                    CIMDGrantedAPIs are the API resources this client can reach because the API grants
                    every Client ID Metadata Document client access, rather than because of spec.apiAccess.
                    Removing an apiAccess entry does not revoke access listed here; that is turned off on
                    the PocketIDAPI. Only ever set for a cimd client.
                  items:
                    type: string
                  type: array
                clientID:
                  description: ClientID is the ID assigned by Pocket-ID
                  type: string
                clientSecretSourceVersion:
                  description: |-
                    ClientSecretSourceVersion identifies the spec.clientSecretRef source last pushed to
                    Pocket-ID. Pocket-ID stores the secret hashed and never returns it, so this is how the
                    operator detects that the referenced Secret changed and needs pushing again.
                  type: string
                clientType:
                  description: |-
                    ClientType is how the client was registered in Pocket-ID: "standard" or "cimd".
                    For a "cimd" client the operator manages only the fields Pocket-ID persists on an
                    admin update; the rest are owned by its OAuth Client ID Metadata Document.
                  type: string
                conditions:
                  description: Conditions represent the current state of the PocketIDOIDCClient resource.
                  items:
                    description: Condition contains details for one aspect of the current state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                createdAt:
                  description: CreatedAt is the creation timestamp from Pocket-ID
                  type: string
                darkLogoReachable:
                  description: |-
                    DarkLogoReachable reports whether Pocket-ID could fetch and store the dark logo from the
                    URL the spec resolves to. Unset when the spec asks for no dark logo.
                  type: boolean
                darkLogoUrl:
                  description: DarkLogoURL is the dark logo URL the operator last successfully applied to Pocket-ID.
                  type: string
                lastRotatedAt:
                  description: |-
                    LastRotatedAt is the time the client secret was last regenerated by the
                    operator. Used to determine when the next scheduled rotation is due.
                  format: date-time
                  type: string
                logoReachable:
                  description: |-
                    LogoReachable reports whether Pocket-ID could fetch and store the light logo from the
                    URL the spec resolves to. Unset when the spec asks for no light logo.
                  type: boolean
                logoUrl:
                  description: LogoURL is the light logo URL the operator last successfully applied to Pocket-ID.
                  type: string
                logoutCallbackUrls:
                  description: LogoutCallbackURLs are the current logout callback URLs from Pocket-ID
                  items:
                    type: string
                  type: array
                managedAPIPermissionIDs:
                  description: |-
                    ManagedAPIPermissionIDs are the Pocket-ID API permission IDs the operator last
                    pushed as this client's API access (both delegated and client-credentials).
                  items:
                    type: string
                  type: array
                managedAPIs:
                  description: ManagedAPIs are the Pocket-ID API IDs the operator last granted this client.
                  items:
                    type: string
                  type: array
                name:
                  description: Name is the resolved name from Pocket-ID
                  type: string
                pkceSupported:
                  description: |-
                    PKCESupported indicates Pocket-ID observed the client using PKCE during an
                    authorization flow while spec.pkceEnabled is false, signalling PKCE can be enabled.
                  type: boolean
                scimProviderID:
                  description: SCIMProviderID is the pocket-id ID of the SCIM service provider for this client, if configured.
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
