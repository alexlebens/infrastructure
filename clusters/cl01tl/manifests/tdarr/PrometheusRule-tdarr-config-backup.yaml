apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: tdarr-config-backup
  namespace: tdarr
  labels:
    app.kubernetes.io/name: tdarr-config-backup
    helm.sh/chart: volsync-target-config-2.4.0
    app.kubernetes.io/instance: tdarr
    app.kubernetes.io/part-of: tdarr
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="tdarr", phase="Failed", pod=~"volsync-.*tdarr-config-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="tdarr", owner_kind="Job", owner_name=~"volsync-.*tdarr-config-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: tdarr-config
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'tdarr-config'.
              Target: tdarr-config-backup
              Namespace: {{ $labels.namespace }}
