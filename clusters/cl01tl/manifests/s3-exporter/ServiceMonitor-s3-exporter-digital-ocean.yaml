apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: s3-exporter-digital-ocean
  labels:
    app.kubernetes.io/instance: s3-exporter
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: s3-exporter
    helm.sh/chart: s3-exporter-5.0.1
  namespace: s3-exporter
spec:
  jobLabel: app.kubernetes.io/name
  namespaceSelector:
    matchNames:
      - s3-exporter
  selector:
    matchLabels:
      app.kubernetes.io/instance: s3-exporter
      app.kubernetes.io/name: s3-exporter
      app.kubernetes.io/service: s3-exporter-digital-ocean
  endpoints:
    - interval: 5m
      path: /metrics
      port: metrics
      scrapeTimeout: 2m
