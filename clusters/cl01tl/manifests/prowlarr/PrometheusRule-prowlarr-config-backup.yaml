apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: prowlarr-config-backup
  namespace: prowlarr
  labels:
    app.kubernetes.io/name: prowlarr-config-backup
    helm.sh/chart: volsync-target-config-2.4.0
    app.kubernetes.io/instance: prowlarr
    app.kubernetes.io/part-of: prowlarr
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="prowlarr", phase="Failed", pod=~"volsync-.*prowlarr-config-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="prowlarr", owner_kind="Job", owner_name=~"volsync-.*prowlarr-config-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: prowlarr-config
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'prowlarr-config'.
              Target: prowlarr-config-backup
              Namespace: {{ $labels.namespace }}
