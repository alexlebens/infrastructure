apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: grafana-operator-postgresql-18-scheduled-backup
  namespace: grafana-operator
  labels:
    app.kubernetes.io/name: grafana-operator-postgresql-18-scheduled-backup
    helm.sh/chart: postgres-18-cluster-8.2.3
    app.kubernetes.io/instance: grafana-operator
    app.kubernetes.io/part-of: grafana-operator
    app.kubernetes.io/version: "v1.30.0"
    app.kubernetes.io/managed-by: Helm
spec:
  immediate: true
  suspend: false
  schedule: "0 0 0 * * *"
  backupOwnerReference: self
  cluster:
    name: grafana-operator-postgresql-18-cluster
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
    parameters:
      barmanObjectName: grafana-operator-postgresql-18-backup
