apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: vaultwarden-data-backup
  namespace: vaultwarden
  labels:
    app.kubernetes.io/name: vaultwarden-data-backup
    helm.sh/chart: volsync-target-data-2.4.0
    app.kubernetes.io/instance: vaultwarden
    app.kubernetes.io/part-of: vaultwarden
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="vaultwarden", phase="Failed", pod=~"volsync-.*vaultwarden-data-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="vaultwarden", owner_kind="Job", owner_name=~"volsync-.*vaultwarden-data-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: vaultwarden-data
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'vaultwarden-data'.
              Target: vaultwarden-data-backup
              Namespace: {{ $labels.namespace }}
