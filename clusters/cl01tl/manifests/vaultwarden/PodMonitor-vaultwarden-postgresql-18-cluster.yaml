apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: vaultwarden-postgresql-18-cluster
  namespace: vaultwarden
  labels:
    app.kubernetes.io/name: vaultwarden-postgresql-18-cluster
    helm.sh/chart: postgres-18-cluster-7.13.3
    app.kubernetes.io/instance: vaultwarden
    app.kubernetes.io/part-of: vaultwarden
    app.kubernetes.io/version: "7.13.3"
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: vaultwarden-postgresql-18-cluster
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
