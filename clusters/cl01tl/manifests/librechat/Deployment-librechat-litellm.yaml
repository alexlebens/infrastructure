apiVersion: apps/v1
kind: Deployment
metadata:
  name: librechat-litellm
  labels:
    app.kubernetes.io/controller: litellm
    app.kubernetes.io/instance: librechat
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: librechat
    helm.sh/chart: librechat-5.1.0
  namespace: librechat
spec:
  revisionHistoryLimit: 3
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/controller: litellm
      app.kubernetes.io/name: librechat
      app.kubernetes.io/instance: librechat
  template:
    metadata:
      annotations:
        checksum/configMaps: c7ed2f4dc79edbbeee64d290dd7eb06ded6bc4b03345d52adf2531ba3ff35a09
      labels:
        app.kubernetes.io/controller: litellm
        app.kubernetes.io/instance: librechat
        app.kubernetes.io/name: librechat
    spec:
      enableServiceLinks: false
      serviceAccountName: librechat
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
      hostIPC: false
      hostNetwork: false
      hostPID: false
      dnsPolicy: ClusterFirst
      containers:
        - args:
            - --config
            - /etc/litellm/config.yaml
            - --port
            - "4000"
          env:
            - name: PORT
              value: "4000"
            - name: LITELLM_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  key: master-key
                  name: litellm-secrets
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  key: gemini-api-key
                  name: litellm-secrets
          image: ghcr.io/berriai/litellm:v1.100.0@sha256:c8756e7b9a61fe45df2ccb5b781d388c3b2f3a21ef9e4956630caef20f9f03aa
          livenessProbe:
            failureThreshold: 5
            httpGet:
              path: /health/liveliness
              port: 4000
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          name: litellm
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /health/readiness
              port: 4000
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            requests:
              cpu: 20m
              memory: 128Mi
          volumeMounts:
            - mountPath: /etc/litellm/config.yaml
              name: litellm-config
              readOnly: true
              subPath: config.yaml
      volumes:
        - configMap:
            name: litellm-config
          name: litellm-config
