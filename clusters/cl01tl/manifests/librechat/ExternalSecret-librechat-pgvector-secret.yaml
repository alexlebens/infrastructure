apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: librechat-pgvector-secret
  namespace: librechat
  labels:
    app.kubernetes.io/name: librechat-pgvector-secret
    app.kubernetes.io/instance: librechat
    app.kubernetes.io/part-of: librechat
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  data:
    - secretKey: password
      remoteRef:
        key: /cl01tl/n8n/postgres-pgvector
        property: password
