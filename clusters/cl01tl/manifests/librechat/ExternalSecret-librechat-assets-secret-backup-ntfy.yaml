apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: librechat-assets-secret-backup-ntfy
  namespace: librechat
  labels:
    app.kubernetes.io/name: librechat-assets-secret-backup-ntfy
    helm.sh/chart: garage-bucket-librechat-assets-0.4.3
    app.kubernetes.io/instance: librechat
    app.kubernetes.io/part-of: librechat
    app.kubernetes.io/version: "v0.7.10"
    app.kubernetes.io/managed-by: Helm
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    template:
      mergePolicy: Merge
      engineVersion: v2
      data:
        ntfy-url: "{{ .url }}/{{ .topic }}"
        ntfy-token: "{{ .token }}"
  data:
    - secretKey: url
      remoteRef:
        key: /cl01tl/ntfy/config
        property: internal-endpoint
    - secretKey: topic
      remoteRef:
        key: /cl01tl/ntfy/topics
        property: rclone
    - secretKey: token
      remoteRef:
        key: /cl01tl/ntfy/users/cl01tl
        property: token
