apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: librechat-meilisearch
  labels:
    helm.sh/chart: meilisearch-0.40.0
    app.kubernetes.io/name: meilisearch
    app.kubernetes.io/instance: librechat
    app.kubernetes.io/version: "v1.54.0"
    app.kubernetes.io/component: search-engine
    app.kubernetes.io/part-of: meilisearch
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  serviceName: librechat-meilisearch
  selector:
    matchLabels:
      app.kubernetes.io/name: meilisearch
      app.kubernetes.io/instance: librechat
  template:
    metadata:
      labels:
        helm.sh/chart: meilisearch-0.40.0
        app.kubernetes.io/name: meilisearch
        app.kubernetes.io/instance: librechat
        app.kubernetes.io/version: "v1.54.0"
        app.kubernetes.io/component: search-engine
        app.kubernetes.io/part-of: meilisearch
        app.kubernetes.io/managed-by: Helm
      annotations:
        checksum/config: cc3ce832e41200be025de42b84966ee08873180adcd22a421a46af161e90b54d
    spec:
      serviceAccountName: librechat-meilisearch
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      volumes:
        - name: tmp
          emptyDir: {}
        - name: data
          persistentVolumeClaim:
            claimName: librechat-meilisearch
      containers:
        - name: meilisearch
          image: "getmeili/meilisearch:v1.54.0"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: data
              mountPath: /meili_data
          envFrom:
            - configMapRef:
                name: librechat-meilisearch-environment
            - secretRef:
                name: librechat-meilisearch-key
          ports:
            - name: http
              containerPort: 7700
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 1
            initialDelaySeconds: 1
            failureThreshold: 60
            timeoutSeconds: 1
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            initialDelaySeconds: 0
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            initialDelaySeconds: 0
            timeoutSeconds: 10
          resources:
            requests:
              cpu: 1m
              memory: 50Mi
