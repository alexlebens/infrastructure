apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: redis-replication-yamtrack
  namespace: yamtrack
  labels:
    helm.sh/chart: redis-replication-1.0.1
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: redis-replication-yamtrack
    app.kubernetes.io/instance: yamtrack
    app.kubernetes.io/part-of: yamtrack
spec:
  selector:
    matchLabels:
      app: redis-replication-yamtrack
      redis_setup_type: replication
      role: replication
  endpoints:
    - port: redis-exporter
      interval: 30s
      scrapeTimeout: 10s
