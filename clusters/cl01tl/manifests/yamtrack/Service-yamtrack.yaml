apiVersion: v1
kind: Service
metadata:
  name: yamtrack
  labels:
    app.kubernetes.io/instance: yamtrack
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: yamtrack
    app.kubernetes.io/service: yamtrack
    helm.sh/chart: yamtrack-4.6.2
  namespace: yamtrack
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/controller: main
    app.kubernetes.io/instance: yamtrack
    app.kubernetes.io/name: yamtrack
