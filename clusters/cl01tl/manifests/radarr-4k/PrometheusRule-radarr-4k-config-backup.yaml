apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: radarr-4k-config-backup
  namespace: radarr-4k
  labels:
    app.kubernetes.io/name: radarr-4k-config-backup
    helm.sh/chart: volsync-target-config-2.4.0
    app.kubernetes.io/instance: radarr-4k
    app.kubernetes.io/part-of: radarr-4k
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="radarr-4k", phase="Failed", pod=~"volsync-.*radarr-4k-config-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="radarr-4k", owner_kind="Job", owner_name=~"volsync-.*radarr-4k-config-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: radarr-4k-config
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'radarr-4k-config'.
              Target: radarr-4k-config-backup
              Namespace: {{ $labels.namespace }}
