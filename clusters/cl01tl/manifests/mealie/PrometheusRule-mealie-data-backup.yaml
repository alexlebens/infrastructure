apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: mealie-data-backup
  namespace: mealie
  labels:
    app.kubernetes.io/name: mealie-data-backup
    helm.sh/chart: volsync-target-data-2.4.0
    app.kubernetes.io/instance: mealie
    app.kubernetes.io/part-of: mealie
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="mealie", phase="Failed", pod=~"volsync-.*mealie-data-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="mealie", owner_kind="Job", owner_name=~"volsync-.*mealie-data-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: mealie-data
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'mealie-data'.
              Target: mealie-data-backup
              Namespace: {{ $labels.namespace }}
