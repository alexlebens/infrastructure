apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: trek-data-backup
  namespace: trek
  labels:
    app.kubernetes.io/name: trek-data-backup
    helm.sh/chart: volsync-target-data-2.4.0
    app.kubernetes.io/instance: trek
    app.kubernetes.io/part-of: trek
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="trek", phase="Failed", pod=~"volsync-.*trek-data-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="trek", owner_kind="Job", owner_name=~"volsync-.*trek-data-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: trek-data
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'trek-data'.
              Target: trek-data-backup
              Namespace: {{ $labels.namespace }}
