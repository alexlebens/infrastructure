apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: houndarr-data-backup
  namespace: houndarr
  labels:
    app.kubernetes.io/name: houndarr-data-backup
    helm.sh/chart: volsync-target-data-2.3.1
    app.kubernetes.io/instance: houndarr
    app.kubernetes.io/part-of: houndarr
    app.kubernetes.io/version: "2.3.1"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            (kube_pod_status_phase{namespace="houndarr", phase="Failed", pod=~"volsync-.*houndarr-data-backup-.*"} == 1)
            * on(namespace, pod) group_left(owner_name)
            kube_pod_owner{namespace="houndarr", owner_kind="Job", owner_name=~"volsync-.*houndarr-data-backup-.*"}
          labels:
            severity: critical
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              VolSync backup pod '{{ $labels.pod }}' for PVC 'houndarr-data' failed.
              Job: {{ $labels.owner_name }}
              Namespace: {{ $labels.namespace }}
