apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: ollama-web-data-backup
  namespace: ollama
  labels:
    app.kubernetes.io/name: ollama-web-data-backup
    helm.sh/chart: volsync-target-data-2.3.1
    app.kubernetes.io/instance: ollama
    app.kubernetes.io/part-of: ollama
    app.kubernetes.io/version: "2.3.1"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            (kube_pod_status_phase{namespace="ollama", phase="Failed", pod=~"volsync-.*ollama-web-data-backup-.*"} == 1)
            * on(namespace, pod) group_left(owner_name)
            kube_pod_owner{namespace="ollama", owner_kind="Job", owner_name=~"volsync-.*ollama-web-data-backup-.*"}
          labels:
            severity: critical
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              VolSync backup pod '{{ $labels.pod }}' for PVC 'ollama-web-data' failed.
              Job: {{ $labels.owner_name }}
              Namespace: {{ $labels.namespace }}
