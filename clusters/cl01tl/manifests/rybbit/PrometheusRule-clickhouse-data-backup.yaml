apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: clickhouse-data-backup
  namespace: rybbit
  labels:
    app.kubernetes.io/name: clickhouse-data-backup
    helm.sh/chart: volsync-target-clickhouse-data-2.4.0
    app.kubernetes.io/instance: rybbit
    app.kubernetes.io/part-of: rybbit
    app.kubernetes.io/version: "2.4.0"
    app.kubernetes.io/managed-by: Helm
spec:
  groups:
    - name: volsync.alerts
      rules:
        - alert: VolSyncBackupPodFailed
          expr: |
            sum by (namespace) (
              (kube_pod_status_phase{namespace="rybbit", phase="Failed", pod=~"volsync-.*clickhouse-data-backup-.*"} == 1)
              * on(namespace, pod) group_left(owner_name)
              kube_pod_owner{namespace="rybbit", owner_kind="Job", owner_name=~"volsync-.*clickhouse-data-backup-.*"}
            ) > 0
          keep_firing_for: 10m
          labels:
            severity: critical
            pvc: clickhouse-data
          annotations:
            summary: "VolSync backup pod failed in {{ $labels.namespace }}"
            description: |
              {{ $value }} VolSync backup pod(s) failed for PVC 'clickhouse-data'.
              Target: clickhouse-data-backup
              Namespace: {{ $labels.namespace }}
