apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cloudnative-pg-postgres-backups-remote-rclone
  labels:
    app.kubernetes.io/instance: cloudnative-pg
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: postgres-backups-remote-rclone
    app.kubernetes.io/version: v1.74.0
    helm.sh/chart: rclone-postgres-backups-remote-0.9.0
  namespace: cloudnative-pg
spec:
  jobLabel: app.kubernetes.io/name
  namespaceSelector:
    matchNames:
      - cloudnative-pg
  selector:
    matchLabels:
      app.kubernetes.io/instance: postgres-backups-remote-rclone
      app.kubernetes.io/name: postgres-backups-remote-rclone
  endpoints:
    - interval: 30s
      path: /metrics
      port: metrics
      scrapeTimeout: 10s
