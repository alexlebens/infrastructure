apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: authentik-postgresql-18-cluster
  namespace: authentik
  labels:
    app.kubernetes.io/name: authentik-postgresql-18-cluster
    helm.sh/chart: postgres-18-cluster-7.15.0
    app.kubernetes.io/instance: authentik
    app.kubernetes.io/part-of: authentik
    app.kubernetes.io/version: "7.15.0"
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: authentik-postgresql-18-cluster
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
