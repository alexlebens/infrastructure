apiVersion: v1
kind: Service
metadata:
  name: memos
  labels:
    app.kubernetes.io/instance: memos
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: memos
    app.kubernetes.io/service: memos
    helm.sh/chart: memos-5.2.1
  namespace: memos
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5230
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/controller: main
    app.kubernetes.io/instance: memos
    app.kubernetes.io/name: memos
