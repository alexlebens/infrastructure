apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: memos-postgresql-18-cluster
  namespace: memos
  labels:
    app.kubernetes.io/name: memos-postgresql-18-cluster
    helm.sh/chart: postgres-18-cluster-8.5.0
    app.kubernetes.io/instance: memos
    app.kubernetes.io/part-of: memos
    app.kubernetes.io/version: "v1.30.1"
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: memos-postgresql-18-cluster
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
