apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: streamystats-postgres-pgvector-backup
  namespace: streamystats
  labels:
    app.kubernetes.io/name: streamystats-postgres-pgvector-backup
    helm.sh/chart: postgres-pgvector-cluster-8.4.3
    app.kubernetes.io/instance: streamystats
    app.kubernetes.io/part-of: streamystats
    app.kubernetes.io/version: "v1.30.0"
    app.kubernetes.io/managed-by: Helm
spec:
  retentionPolicy: 7d
  instanceSidecarConfiguration:
    env:
      - name: AWS_REQUEST_CHECKSUM_CALCULATION
        value: when_required
      - name: AWS_RESPONSE_CHECKSUM_VALIDATION
        value: when_required
  configuration:
    destinationPath: s3://postgres-backups/cl01tl/streamystats/streamystats-postgres-pgvector-cluster
    endpointURL: http://garage-cluster-a.garage-operator.svc.cluster.local:3900
    wal:
      compression: snappy
      maxParallel: 4
    data:
      compression: snappy
      jobs: 4
    s3Credentials:
      accessKeyId:
        name: streamystats-postgres-pgvector-backup-secret
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: streamystats-postgres-pgvector-backup-secret
        key: ACCESS_SECRET_KEY
      region:
        name: streamystats-postgres-pgvector-backup-secret
        key: ACCESS_REGION
