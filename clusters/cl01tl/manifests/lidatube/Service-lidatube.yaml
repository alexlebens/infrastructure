apiVersion: v1
kind: Service
metadata:
  name: lidatube
  labels:
    app.kubernetes.io/instance: lidatube
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: lidatube
    app.kubernetes.io/service: lidatube
    helm.sh/chart: lidatube-4.6.2
  namespace: lidatube
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/controller: main
    app.kubernetes.io/instance: lidatube
    app.kubernetes.io/name: lidatube
