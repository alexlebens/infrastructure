redis-operator:
  redisOperator:
    imageName: ghcr.io/ot-container-kit/redis-operator/redis-operator
    imageTag: v0.21.0
    metrics:
      enabled: true
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
  replicas: 2
  certmanager:
    enabled: false
  manager:
    config:
      kubeClientTimeout: null
