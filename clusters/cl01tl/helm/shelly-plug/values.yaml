shelly-plug:
  controllers:
    main:
      type: deployment
      replicas: 1
      strategy: Recreate
      initContainers:
        init-fetch-repo:
          securityContext:
            runAsUser: 0
          image:
            repository: alpine/git
            tag: v2.54.0@sha256:113d99116e236f93f0b1f53cd46dbda662cf1136d20dc9ae2834962226654d9f
          command:
            - /bin/sh
            - -ec
            - |
              cd /var/www/html
              if [ -d ".git" ]; then
                echo "Git repository found. Pulling latest changes..."
                git pull
              else
                echo "Not a git repository. Initializing ..."
                git init
                git remote add origin https://github.com/geerlingguy/shelly-plug-prometheus.git
                git fetch origin
                git checkout origin/master -ft
              fi
      containers:
        main:
          image:
            repository: php
            tag: 8.5.9-apache@sha256:52c2e3ecd37f52936a46109720adbda74f12788580906fa8b3eaf07ba360dfbe
          env:
            - name: SHELLY_HOSTNAME
              value: it05sp.alexlebens.net
            - name: SHELLY_GENERATION
              value: 2
          envFrom:
            - secretRef:
                name: shelly-plug-config-secret
          resources:
            requests:
              cpu: 1m
              memory: 20Mi
  service:
    main:
      controller: main
      ports:
        metrics:
          port: 80
          targetPort: 80
  serviceMonitor:
    main:
      selector:
        matchLabels:
          app.kubernetes.io/name: shelly-plug
          app.kubernetes.io/instance: shelly-plug
      serviceName: '{{ include "bjw-s.common.lib.chart.names.fullname" $ }}'
      endpoints:
        - port: metrics
          interval: 30s
          scrapeTimeout: 10s
          path: /metrics
  persistence:
    script:
      storageClass: ceph-block
      accessMode: ReadWriteOnce
      size: 1Gi
      advancedMounts:
        main:
          init-fetch-repo:
            - path: /var/www/html
              readOnly: false
          main:
            - path: /var/www/html
              readOnly: false
