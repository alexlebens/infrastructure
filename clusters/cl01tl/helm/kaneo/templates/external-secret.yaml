apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kaneo-key
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: kaneo-key
    {{- include "custom.labels" . | nindent 4 }}
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  data:
    - secretKey: auth-secret
      remoteRef:
        key: /cl01tl/kaneo/key
        property: auth-secret

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kaneo-oidc-secret
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: kaneo-oidc-secret
    {{- include "custom.labels" . | nindent 4 }}
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  data:
    - secretKey: clientId
      remoteRef:
        key: /cl01tl/pocket-id/oidc/kaneo
        property: clientId
    - secretKey: clientSecret
      remoteRef:
        key: /cl01tl/pocket-id/oidc/kaneo
        property: clientSecret

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kaneo-bucket-garage
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: kaneo-bucket-garage
    {{- include "custom.labels" . | nindent 4 }}
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  data:
    - secretKey: S3_ACCESS_KEY_ID
      remoteRef:
        key: /garage/home-infra/kaneo-assets
        property: ACCESS_KEY_ID
    - secretKey: S3_SECRET_ACCESS_KEY
      remoteRef:
        key: /garage/home-infra/kaneo-assets
        property: ACCESS_SECRET_KEY
    - secretKey: S3_REGION
      remoteRef:
        key: /garage/home-infra/kaneo-assets
        property: ACCESS_REGION
