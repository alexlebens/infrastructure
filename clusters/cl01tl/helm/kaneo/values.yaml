kaneo:
  controllers:
    main:
      type: deployment
      strategy: Recreate
      containers:
        main:
          image:
            repository: ghcr.io/usekaneo/kaneo
            tag: 2.17.5@sha256:1addfad5ce9ab814faca020c0ffef68c13244434cd287d099bf0d942f7b4556f
          env:
            - name: KANEO_CLIENT_URL
              value: https://kaneo.alexlebens.net
            - name: KANEO_API_URL
              value: https://kaneo.alexlebens.net
            - name: AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: kaneo-key
                  key: auth-secret
            - name: DATABASE_HOST
              valueFrom:
                secretKeyRef:
                  name: kaneo-postgresql-18-cluster-app
                  key: host
            - name: DATABASE_PORT
              valueFrom:
                secretKeyRef:
                  name: kaneo-postgresql-18-cluster-app
                  key: port
            - name: DATABASE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: kaneo-postgresql-18-cluster-app
                  key: user
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kaneo-postgresql-18-cluster-app
                  key: password
            - name: DATABASE_NAME
              valueFrom:
                secretKeyRef:
                  name: kaneo-postgresql-18-cluster-app
                  key: dbname
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: kaneo-postgresql-18-cluster-app
                  key: uri
            - name: REDIS_URL
              value: redis://kaneo-valkey.kaneo:6379
            - name: S3_BUCKET
              value: kaneo-assets
            - name: S3_FORCE_PATH_STYLE
              value: "true"
            - name: S3_ENDPOINT
              value: https://s3-a.alexlebens.net
            - name: S3_REGION
              valueFrom:
                secretKeyRef:
                  name: kaneo-assets-key
                  key: region
            - name: S3_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: kaneo-assets-key
                  key: access-key-id
            - name: S3_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: kaneo-assets-key
                  key: secret-access-key
            - name: CUSTOM_OAUTH_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: kaneo-oidc-secret
                  key: clientId
            - name: CUSTOM_OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: kaneo-oidc-secret
                  key: clientSecret
            - name: CUSTOM_OAUTH_AUTHORIZATION_URL
              value: https://pocket-id.alexlebens.dev/authorize
            - name: CUSTOM_OAUTH_TOKEN_URL
              value: https://pocket-id.alexlebens.dev/api/oidc/token
            - name: CUSTOM_OAUTH_USER_INFO_URL
              value: https://pocket-id.alexlebens.dev/api/oidc/userinfo
            - name: CUSTOM_OAUTH_LOGOUT_URL
              value: https://pocket-id.alexlebens.dev/logout
            - name: CUSTOM_OAUTH_DISCOVERY_URL
              value: https://pocket-id.alexlebens.dev/.well-known/openid-configuration
            - name: CUSTOM_OAUTH_SCOPES
              value: openid,profile,email
            - name: CUSTOM_OAUTH_AUTO_LOGIN
              value: "true"
            - name: CUSTOM_AUTH_PKCE
              value: "true"
          probes:
            liveness:
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /api/health
                  port: 5173
          resources:
            requests:
              cpu: 10m
              memory: 120Mi
  service:
    main:
      ports:
        http:
          port: 80
          targetPort: 5173
  route:
    main:
      kind: HTTPRoute
      parentRefs:
        - name: traefik-gateway
          namespace: traefik
      hostnames:
        - kaneo.alexlebens.net
      rules:
        - backendRefs:
            - name: kaneo
              port: 80
postgres-18-cluster:
  mode: recovery
  recovery:
    objectStore:
      index: 1
  backup:
    objectStore:
      index: 1
    scheduledBackups:
      suspend: false
      schedule: "0 20 16 * * *"
garage-bucket-kaneo-assets:
  bucketName: kaneo-assets
  cors:
    enabled: true
    policy: |
      {
        "CORSRules": [
          {
            "AllowedOrigins": ["https://kaneo.alexlebens.net"],
            "AllowedHeaders": ["*"],
            "AllowedMethods": ["GET", "PUT", "POST", "DELETE", "HEAD"],
            "MaxAgeSeconds": 3000,
            "ExposeHeaders": ["ETag"]
          }
        ]
      }
  backups:
    primary:
      enabled: true
      cronJob:
        suspend: false
        schedule: 0 0 * * *
      destination:
        bucketName: kaneo-assets
    secondary:
      enabled: true
      cronJob:
        suspend: false
        schedule: 0 0 * * *
      destination:
        bucketName: kaneo-asssets-47edc3f232f2797f
