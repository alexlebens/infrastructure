apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: n8n-sandbox-selfsigned-issuer
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: n8n-sandbox-selfsigned-issuer
    {{- include "custom.labels" . | nindent 4 }}
spec:
  selfSigned: {}
