plex:
  controllers:
    main:
      type: deployment
      replicas: 1
      strategy: Recreate
      annotations:
        prometheus.io/probe-module: http_401
      pod:
        securityContext:
          fsGroup: 1000
          fsGroupChangePolicy: OnRootMismatch
        affinity:
          podAntiAffinity:
            requiredDuringSchedulingIgnoredDuringExecution:
              - labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - jellyfin
                topologyKey: kubernetes.io/hostname
      containers:
        main:
          image:
            repository: ghcr.io/linuxserver/plex
            tag: 1.43.3.10861-07dfddaeb-ls318@sha256:d8b3417b9173bab8bb79b167119b6b69a85558f2127e7fa810a045654e3dbe7f
          env:
            - name: TZ
              value: America/Chicago
            - name: VERSION
              value: docker
          resources:
            limits:
              gpu.intel.com/i915: 1
            requests:
              gpu.intel.com/i915: 1
              cpu: 10m
              memory: 100Mi
  service:
    main:
      controller: main
      type: LoadBalancer
      annotations:
        lbipam.cilium.io/ips: "10.232.1.33"
      ports:
        http:
          port: 32400
          targetPort: 32400
  route:
    main:
      kind: HTTPRoute
      parentRefs:
        - group: gateway.networking.k8s.io
          kind: Gateway
          name: traefik-gateway
          namespace: traefik
      hostnames:
        - plex.alexlebens.net
      rules:
        - backendRefs:
            - name: plex
              port: 32400
          matches:
            - path:
                type: PathPrefix
                value: /
  persistence:
    config:
      forceRename: plex-config
      storageClass: ceph-block
      accessMode: ReadWriteOnce
      size: 200Gi
      advancedMounts:
        main:
          main:
            - path: /config
              readOnly: false
    transcode:
      type: emptyDir
      advancedMounts:
        main:
          main:
            - path: /transcode
              readOnly: false
    media:
      existingClaim: plex-nfs-storage
      advancedMounts:
        main:
          main:
            - path: /mnt/store
              readOnly: true
