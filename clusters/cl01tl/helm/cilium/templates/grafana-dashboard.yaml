apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cilium-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: cilium-dashboard
    {{- include "custom.labels" $ | nindent 4 }}
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      app: grafana-main
  folderUID: grafana-folder-system
  configMapRef:
    name: cilium-dashboard
    key: cilium-dashboard.json

---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cilium-operator-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: cilium-operator-dashboard
    {{- include "custom.labels" $ | nindent 4 }}
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      app: grafana-main
  folderUID: grafana-folder-system
  configMapRef:
    name: cilium-operator-dashboard
    key: cilium-operator-dashboard.json

---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: hubble-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: hubble-dashboard
    {{- include "custom.labels" $ | nindent 4 }}
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      app: grafana-main
  folderUID: grafana-folder-system
  configMapRef:
    name: hubble-dashboard
    key: hubble-dashboard.json
