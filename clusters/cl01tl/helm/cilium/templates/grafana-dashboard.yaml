apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cilium-agent
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: cilium-agent
    {{- include "custom.labels" $ | nindent 4 }}
spec:
  instanceSelector:
    matchLabels:
      app: grafana-main
  folderUID: grafana-folder-system
  configMapRef:
    name: cilium-agent-dashboard
    key: cilium-agent.json

---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cilium-operator
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: cilium-agent
    {{- include "custom.labels" $ | nindent 4 }}
spec:
  instanceSelector:
    matchLabels:
      app: grafana-main
  folderUID: grafana-folder-system
  configMapRef:
    name: cilium-operator-dashboard
    key: cilium-operator.json

---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: hubble
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: cilium-agent
    {{- include "custom.labels" $ | nindent 4 }}
spec:
  instanceSelector:
    matchLabels:
      app: grafana-main
  folderUID: grafana-folder-system
  configMapRef:
    name: hubble-dashboard
    key: hubble.json

---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cilium-envoy
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: cilium-agent
    {{- include "custom.labels" $ | nindent 4 }}
spec:
  instanceSelector:
    matchLabels:
      app: grafana-main
  folderUID: grafana-folder-system
  configMapRef:
    name: cilium-envoy-dashboard
    key: cilium-envoy.json
