psmdb-operator:
  disableTelemetry: true
  watchAllNamespaces: true
garage-bucket-mongodb-backups:
  bucketName: mongodb-backups
  backups:
    primary:
      enabled: true
      cronJob:
        suspend: false
        schedule: 0 0 * * *
      destination:
        bucketName: mongodb-backups
