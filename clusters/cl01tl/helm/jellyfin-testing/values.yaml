jellyfin-testing:
  image:
    repository: ghcr.io/jellyfin/jellyfin
    tag: preview@sha256:669d234c776a37b331d875f58246434f80dbbbb619fa7edef55ce26d187baa2c
  podSecurityContext:
    fsGroup: 1000
    fsGroupChangePolicy: OnRootMismatch
  eploymentStrategy:
    type: Recreate
  httpRoute:
    enabled: true
    parentRefs:
      - name: traefik-gateway
        namespace: traefik
    hostnames:
      - jellyfin-testing.alexlebens.net
  resources:
    limits:
      gpu.intel.com/i915: 1
    requests:
      gpu.intel.com/i915: 1
      cpu: 100m
      memory: 100Mi
  volumes:
    - name: media-nfs
      persistentVolumeClaim:
        claimName: jellyfin-testing-nfs-storage
    - name: youtube-nfs
      persistentVolumeClaim:
        claimName: jellyfin-testing-youtube-nfs-storage
  volumeMounts:
    - mountPath: /mnt/store
      name: media-nfs
      readOnly: true
    - mountPath: /mnt/youtube
      name: youtube-nfs
      readOnly: true
  affinity:
    podAntiAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        - labelSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - jellyfin
          topologyKey: kubernetes.io/hostname
  jellyfin:
    env:
      - name: TZ
        value: America/Chicago
      - name: JELLYFIN_PublishedServerUrl
        value: https://jellyfin-testing.alexlebens.net
  persistence:
    config:
      accessMode: ReadWriteOnce
      size: 50Gi
      storageClass: ceph-block
    media:
      enabled: false
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
meilisearch:
  environment:
    MEILI_NO_ANALYTICS: true
    MEILI_ENV: production
    MEILI_UPGRADE_DB: true
  auth:
    existingMasterKeySecret: jellyfin-testing-meilisearch-key
  persistence:
    enabled: true
    storageClass: ceph-block
    size: 5Gi
  resources:
    requests:
      cpu: 10m
      memory: 1000Mi
  serviceMonitor:
    enabled: true
