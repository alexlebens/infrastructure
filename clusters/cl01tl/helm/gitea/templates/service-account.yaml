apiVersion: v1
kind: ServiceAccount
metadata:
  name: buildx-runner
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: buildx-runner
    {{- include "custom.labels" . | nindent 4 }}
