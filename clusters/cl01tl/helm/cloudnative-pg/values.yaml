cloudnative-pg:
  replicaCount: 2
  resources:
    requests:
      cpu: 5m
      memory: 100Mi
  monitoring:
    podMonitorEnabled: true
plugin-barman-cloud:
  replicaCount: 1
  crds:
    create: true
  resources:
    requests:
      cpu: 1m
      memory: 30Mi
garage-bucket-postgres-backups:
  bucketName: postgres-backups
  backups:
    primary:
      enabled: false
      cronJob:
        suspend: false
        schedule: 0 0 * * *
      destination:
        bucketName: postgres-backups
    secondary:
      enabled: false
      cronJob:
        suspend: false
        schedule: 0 0 * * *
      destination:
        bucketName: postgres-backups-775957147abfbc73
