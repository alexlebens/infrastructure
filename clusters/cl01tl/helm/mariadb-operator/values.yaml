mariadb-operator:
  ha:
    replicas: 1
  metrics:
    enabled: true
  pdb:
    enabled: true
garage-bucket-mariadb-backups:
  bucketName: mariadb-backups
  backups:
    primary:
      enabled: false
      cronJob:
        suspend: false
        schedule: 0 0 * * *
      destination:
        bucketName: mariadb-backups
    secondary:
      enabled: false
      cronJob:
        suspend: false
        schedule: 0 0 * * *
      destination:
        bucketName: mariadb-backups-6e3b78870f7af040
