apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.unpackerr.persistence.storage.existingClaim }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.unpackerr.persistence.storage.existingClaim }}
    {{- include "custom.labels" . | nindent 4 }}
spec:
  volumeName: {{ .Values.unpackerr.persistence.storage.existingClaim }}
  storageClassName: nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
