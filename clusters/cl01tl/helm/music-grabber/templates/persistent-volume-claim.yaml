apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ index .Values "music-grabber" "persistence" "music" "existingClaim" }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ index .Values "music-grabber" "persistence" "music" "existingClaim" }}
    {{- include "custom.labels" . | nindent 4 }}
spec:
  volumeName: {{ index .Values "music-grabber" "persistence" "music" "existingClaim" }}
  storageClassName: nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
