actual:
  controllers:
    main:
      type: deployment
      strategy: Recreate
      containers:
        main:
          image:
            repository: ghcr.io/actualbudget/actual
            tag: 26.8.1@sha256:6478d9ddfc0924479c09e6699c205e354c6f2216dfe7de3c0fb7b590d6edcdc5
          env:
            - name: ACTUAL_PORT
              value: 5006
            - name: ACTUAL_USER_CREATION_MODE
              value: "login"
            - name: ACTUAL_OPENID_DISCOVERY_URL
              value: https://auth.alexlebens.dev/.well-known/openid-configuration
            - name: ACTUAL_OPENID_SERVER_HOSTNAME
              value: https://actual.alexlebens.dev
            - name: ACTUAL_OPENID_CLIENT_ID
              value: 60AE6E81-9F22-4E5D-8F8F-0816D8646B5C
            - name: ACTUAL_OPENID_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: actual-oidc-secret
                  key: clientSecret
          resources:
            requests:
              cpu: 25m
              memory: 50Mi
          probes:
            liveness:
              enabled: true
              custom: true
              spec:
                exec:
                  command:
                    - /usr/bin/env
                    - bash
                    - -c
                    - node src/scripts/health-check.js
                initialDelaySeconds: 60
                timeoutSeconds: 10
  service:
    main:
      ports:
        http:
          port: 80
          targetPort: 5006
  route:
    main:
      kind: HTTPRoute
      parentRefs:
        - name: traefik-gateway
          namespace: traefik
      hostnames:
        - actual.alexlebens.dev
      rules:
        - backendRefs:
            - name: actual
              port: 80
  persistence:
    data:
      forceRename: actual-data
      storageClass: ceph-block
      accessMode: ReadWriteOnce
      size: 2Gi
      advancedMounts:
        main:
          main:
            - path: /data
volsync-target-data:
  pvcTarget: actual-data
  local:
    enabled: true
    schedule: 0 8 * * *
  remote:
    enabled: true
    schedule: 0 10 * * 0
  external:
    enabled: true
    schedule: 0 9 * * 0
