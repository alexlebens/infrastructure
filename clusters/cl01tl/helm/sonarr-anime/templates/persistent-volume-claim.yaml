apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ (index .Values "sonarr-anime").persistence.media.existingClaim }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ (index .Values "sonarr-anime").persistence.media.existingClaim }}
    {{- include "custom.labels" . | nindent 4 }}
spec:
  volumeName: {{ (index .Values "sonarr-anime").persistence.media.existingClaim }}
  storageClassName: nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
