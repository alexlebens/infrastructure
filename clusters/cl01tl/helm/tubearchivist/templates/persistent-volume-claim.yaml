apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.tubearchivist.persistence.youtube.existingClaim }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.tubearchivist.persistence.youtube.existingClaim }}
    {{- include "custom.labels" . | nindent 4 }}
spec:
  volumeName: {{ .Values.tubearchivist.persistence.youtube.existingClaim }}
  storageClassName: nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
