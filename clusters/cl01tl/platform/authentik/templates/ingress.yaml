apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authentik-cl01tl-authentik-server
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: authentik-cl01tl-authentik-server
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion }}
    app.kubernetes.io/component: tailscale
    app.kubernetes.io/part-of: {{ .Release.Name }}
  annotations:
    tailscale.com/experimental-forward-cluster-traffic-via-ingress: "true"
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - auth-cl01tl
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authentik-server
                port:
                  name: http

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authentik-cl01tl-authentik-test
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: authentik-cl01tl-authentik-test
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion }}
    app.kubernetes.io/component: cilium
    app.kubernetes.io/part-of: {{ .Release.Name }}
spec:
  ingressClassName: cilium
  tls:
    - hosts:
        - auth.alexlebens.net
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authentik-server
                port:
                  name: http
